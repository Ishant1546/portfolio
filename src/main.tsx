import React from 'react'
import ReactDOM from 'react-dom/client'
import MinecraftPortfolio from './minecraft-portfolio'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MinecraftPortfolio />
  </React.StrictMode>,
)
